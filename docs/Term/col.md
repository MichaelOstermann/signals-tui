# col

A read-only signal containing the current cursor column position.

## Example

```ts
import { Term } from "@monstermann/signals-tui";

const c = Term.col();
console.log(c); // Current column
```
