# width

A memo signal that returns the current terminal width in columns.

## Example

```ts
import { Term } from "@monstermann/signals-tui";

const w = Term.width();
console.log(w); // Terminal width
```
